# Python: referencia de un minuto

```python
# Variables y salida
nombre = "Johan"
print(f"Hola, {nombre}")

# Condición
if edad >= 18:
    print("OK")

# Iterar
for item in lista:
    print(item)

# Lista y diccionario
numeros.append(10)
valor = datos.get("clave", "valor por defecto")

# Comprensión
pares = [n for n in range(10) if n % 2 == 0]

# Función
def sumar(a, b):
    return a + b

# Archivo
with open("archivo.txt", encoding="utf-8") as f:
    texto = f.read()

# Error
try:
    valor = int(texto)
except ValueError:
    pass
```

Comandos: `python -m venv .venv`, activar el entorno y usar `pip install paquete`.
